/* This file is automatically generated by ABP framework to use MVC Controllers from javascript. */


// module bookStore

(function(){

  // controller acme.bookStore.controllers.book

  (function(){

    abp.utils.createNamespace(window, 'acme.bookStore.controllers.book');

    acme.bookStore.controllers.book.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + '' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    acme.bookStore.controllers.book.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/BookStore/book' + abp.utils.buildQueryString([{ name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

    acme.bookStore.controllers.book.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/BookStore/book',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    acme.bookStore.controllers.book.update = function(id, input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + '' + id + '',
        type: 'PUT',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    acme.bookStore.controllers.book['delete'] = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + '' + id + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

  })();

})();


